//given number is Armstrong or not//
#include<stdio.h>
int main()
{
	int n,r,sum=0,m;
	scanf("%d",&n);
	m=n;
	while(n>0)
	{
		r=n%10;
		sum=sum+(r*r*r);
		n=n/10;
		
	}
	if(sum==m)
	printf("ARMSTRONG NUMBER");
	else
	printf("NOT AN ARMSTRONG NUMBER");
	return 0;
}
